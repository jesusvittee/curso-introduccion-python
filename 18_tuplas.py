numbers = (1,2,3,4,5,6,7,8)
strings = ('Fud', 'gamesa', 'bimbo')

#tuple no permite usar uppend, remove, index
# es posible, usar .index .count y
print(numbers[4])
print(numbers.count(5))
#convertir de tuple a lista
list = list(strings)
#lista a tuple
tuple = tuple(list)